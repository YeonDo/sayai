--- src/main/resources/templates/fantasy/draft.html
+++ src/main/resources/templates/fantasy/draft.html
@@ -35,6 +35,22 @@
         }
         .timer { font-weight: bold; color: #d9534f; }
         .disabled-btn { opacity: 0.5; cursor: not-allowed; }
+
+        .team-restriction-list {
+            display: flex;
+            flex-wrap: wrap;
+            gap: 5px;
+            margin-top: 10px;
+        }
+        .team-restriction-badge {
+            padding: 3px 8px;
+            border-radius: 4px;
+            background-color: #f1f3f5;
+            font-size: 0.85rem;
+            color: #495057;
+            border: 1px solid #ced4da;
+            transition: all 0.2s;
+        }
+        .team-restriction-badge.picked {
+            background-color: #e9ecef;
+            color: #adb5bd;
+            text-decoration: line-through;
+            border-color: #dee2e6;
+        }
     </style>
 </head>
 <body>
@@ -53,6 +69,12 @@
                 <span>Time Left: <span id="timer" class="timer">--:--</span></span>
                 <span id="salary-info" style="display:none; margin-left: 10px; font-weight: bold; color: #2e7d32;">Salary: <span id="current-cost">0</span> / <span id="salary-cap"></span></span>
             </div>
+
+            <!-- Team Restriction Display -->
+            <div id="team-restriction-container" style="display:none; padding: 10px; background: #fff; border: 1px solid #ddd; border-radius: 5px; margin-bottom: 10px;">
+                <strong style="font-size: 0.9rem; color: #666;">팀 제한 (10개 구단 1명씩 픽)</strong>
+                <div id="team-restriction-list" class="team-restriction-list"></div>
+            </div>
         </div>


@@ -192,7 +214,8 @@
             myPreferredTeam: null,
             round: 1, // Default
             salaryCap: null,
-            currentCost: 0
+            currentCost: 0,
+            useTeamRestriction: false
         };

         // Client-side cache
@@ -231,6 +254,12 @@
             $(`#tab-${tabName}`).addClass('active');
         }

+        const KBO_TEAMS = ["KIA", "LG", "KT", "SSG", "NC", "두산", "롯데", "삼성", "한화", "키움"];
+
         function initGame() {
             $.get(`/apis/v1/fantasy/games/${gameSeq}/details`, function(data) {
                 gameState.status = data.status;
@@ -242,6 +271,7 @@
                 gameState.round = data.roundNum;
                 gameState.salaryCap = data.salaryCap;
                 gameState.useFirstPickRule = data.useFirstPickRule;
+                gameState.useTeamRestriction = data.useTeamRestriction;

                 // Determine My Info
                 const myPart = gameState.participants.find(p => String(p.participantId) === String(userId));
@@ -254,6 +284,10 @@
                     $('#salary-info').show();
                     $('#salary-cap').text(gameState.salaryCap);
                 }
+
+                if (gameState.useTeamRestriction) {
+                    $('#team-restriction-container').show();
+                }

                 updateUI();
                 loadAvailablePlayers(true); // Force refresh on init/resume
@@ -432,6 +466,7 @@
             $.get(`/apis/v1/fantasy/games/${gameSeq}/players/${userId}/picks`, function(data) {
                 const list = $('#my-picks-list');
                 list.empty();
+                const pickedTeams = new Set();
                 let totalCost = data.currentCost;
                 data.picks.forEach(player => {
                     const firstPos = player.position ? player.position.split(',')[0].trim().toLowerCase() : '';
@@ -440,6 +475,8 @@

                     list.append(`
                         <li style="padding: 8px 0; border-bottom: 1px solid #eee;">
                             <strong>${player.name}</strong> <span class="position-badge ${cssClass}">${player.position}</span>
                             <span style="float:right; font-size:0.9rem; color:#666;">${player.cost || 0}</span>
                         </li>
                     `);
+                    if (player.team) {
+                        pickedTeams.add(player.team.trim());
+                    }
                 });
                 $('#current-cost').text(totalCost);
                 gameState.currentCost = totalCost;
                 // Reload available players to update Over Cap buttons if cost changed
@@ -448,6 +485,27 @@
                 // We call loadAvailablePlayers here to refresh buttons state
                 loadAvailablePlayers();
+
+                // Update Team Restriction UI if enabled
+                if (gameState.useTeamRestriction) {
+                    renderTeamRestriction(pickedTeams);
+                }
             });
         }

+        function renderTeamRestriction(pickedTeams) {
+            const container = $('#team-restriction-list');
+            container.empty();
+
+            KBO_TEAMS.forEach(team => {
+                let isPicked = false;
+                for (let pickedTeam of pickedTeams) {
+                    if (pickedTeam.includes(team) || team.includes(pickedTeam)) {
+                        isPicked = true;
+                        break;
+                    }
+                }
+
+                const badgeClass = isPicked ? 'team-restriction-badge picked' : 'team-restriction-badge';
+                container.append(`<span class="${badgeClass}">${team}</span>`);
+            });
+        }
+
         function draftPlayer(playerSeq) {
